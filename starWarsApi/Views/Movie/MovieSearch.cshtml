@model List<Tuple<string, string>>
@{
    ViewBag.Title = "MovieSearch";
}

<h2>Movie Search Result</h2>

@for (int i = 0; i < Model.Count; i++)
{
    <br />
    @Html.ActionLink(Model.ElementAt(i).Item1, "SelectedMovie", new { link = Model.ElementAt(i).Item2 })
    <br />
}